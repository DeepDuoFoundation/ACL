[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "litho-digital-twin"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/litho_digital_twin"]

[project.optional-dependencies]
test = ["pytest>=8.0.0"]
